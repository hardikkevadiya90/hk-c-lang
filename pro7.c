// are of triangle 
#include<stdio.h>
void main()
{
    float height,base,area;
    printf("enter the height:");
    scanf("%f",&height);
    printf("enter the base:");
    scanf("%f",&base);
    area=height*base/2;
    printf("area of triangle is :%f",area);

    
}